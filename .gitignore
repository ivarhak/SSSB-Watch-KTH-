# runtime output — regenerated by every scrape. Committing this shipped stale
# test data to fresh clones, which --serve then served as if it were current.
data/current_listings.json
debug_page.html
__pycache__/
*.pyc
venv/

# local AI/session context — useful to keep, not worth publishing
CLAUDE.md
